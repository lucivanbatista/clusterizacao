package logic;

public class MapMatching {
	
	//Criar o array de vertices (dao)
	
	//metodo 1
	//pegar do dao todos os vertices (table_vertices)
	// para cada ponto já clusterizado
	// ponto.id = findVertice(ponto.id)
	
}
